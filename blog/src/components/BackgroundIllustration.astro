<section class="bg-illustration" aria-hidden="true">
  <svg viewBox="0 0 1600 900" class="bg-illustration__svg">
    <g class="float-slow">
      <ellipse cx="220" cy="180" rx="180" ry="120" class="blob blob-1" />
      <circle cx="150" cy="320" r="26" class="dot dot-1" />
      <circle cx="320" cy="360" r="18" class="dot dot-2" />
    </g>

    <g class="float-medium">
      <ellipse cx="1120" cy="220" rx="250" ry="150" class="blob blob-2" />
      <circle cx="1360" cy="180" r="22" class="dot dot-3" />
      <circle cx="1280" cy="380" r="28" class="dot dot-4" />
    </g>

    <g class="float-fast">
      <ellipse cx="980" cy="520" rx="150" ry="95" class="blob blob-3" />
      <rect x="920" y="490" width="80" height="60" rx="10" class="icon icon-card" />
      <circle cx="1080" cy="560" r="24" class="icon icon-circle" />
    </g>

    <g class="scribble-group rotate-soft">
      <path
        d="M540 220 C560 180, 640 180, 660 230
           C680 280, 600 310, 560 280
           C520 255, 520 240, 540 220 Z"
        class="cloud"
      />
      <path
        d="M565 235
           C575 215, 610 215, 620 238
           C628 256, 605 270, 587 262
           C566 252, 557 247, 565 235 Z"
        class="scribble"
      />
    </g>
  </svg>
</section>

<style>
  .bg-illustration {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .bg-illustration__svg {
    width: 100%;
    height: 100%;
  }

  .blob {
    fill: rgba(139, 92, 246, 0.16);
  }

  .blob-1 { fill: rgba(199, 166, 255, 0.16); }
  .blob-2 { fill: rgba(168, 85, 247, 0.16); }
  .blob-3 { fill: rgba(216, 180, 254, 0.14); }

  .dot {
    fill: rgba(234, 220, 255, 0.9);
  }

  .icon {
    fill: rgba(234, 220, 255, 0.28);
    stroke: rgba(255,255,255,0.45);
    stroke-width: 2;
  }

  .cloud {
    fill: rgba(255,255,255,0.92);
  }

  .scribble {
    fill: none;
    stroke: #2b1238;
    stroke-width: 6;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .float-slow {
    animation: floatSlow 10s ease-in-out infinite alternate;
  }

  .float-medium {
    animation: floatMedium 8s ease-in-out infinite alternate;
  }

  .float-fast {
    animation: floatFast 6s ease-in-out infinite alternate;
  }

  .rotate-soft {
    transform-origin: 600px 240px;
    animation: rotateSoft 9s ease-in-out infinite alternate;
  }

  @keyframes floatSlow {
    from { transform: translateY(0px) translateX(0px); }
    to { transform: translateY(-12px) translateX(10px); }
  }

  @keyframes floatMedium {
    from { transform: translateY(0px) translateX(0px); }
    to { transform: translateY(10px) translateX(-12px); }
  }

  @keyframes floatFast {
    from { transform: translateY(0px) scale(1); }
    to { transform: translateY(-8px) scale(1.02); }
  }

  @keyframes rotateSoft {
    from { transform: rotate(-2deg); }
    to { transform: rotate(2deg); }
  }
</style>